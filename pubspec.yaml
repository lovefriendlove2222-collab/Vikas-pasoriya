name: vikas_pasoriya
description: "A new Flutter project."

# ऐप का वर्जन
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # जरूरी प्लगइन्स (Firebase आदि)
  cupertino_icons: ^1.0.6
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # अगर फोटो या फोंट इस्तेमाल करने हों तो यहाँ नीचे जोड़ दियो
  # assets:
  #   - assets/images/
