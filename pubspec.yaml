name: vikas_pasoria_app
description: Professional Multi-Org Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.1
  cloud_firestore: ^4.9.1
  firebase_auth: ^4.7.0
  intl: ^0.18.1
  http: ^1.1.0

flutter:
  uses-material-design: true
